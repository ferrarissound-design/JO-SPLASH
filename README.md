# JO-SPLASH — CHROMA DUEL

Three.js 製、ブラウザで動作する 1 対 1 のインク陣取り FPS ゲームです。
床をインクで塗り合い、制限時間(90秒)終了時により広い面積を塗っていた側が勝利します。

## 起動方法

ビルドツールを使わない素の ES Modules 構成です。ブラウザの `fetch`/モジュール読み込みは
`file://` では動作しないため、任意の静的ファイルサーバーで配信してください。

```bash
# 例1: Python
python3 -m http.server 8080

# 例2: Node (npx)
npx serve .
```

起動後、ブラウザで `http://localhost:8080/index.html` を開いてください。

## 操作方法

| 操作 | 内容 |
| --- | --- |
| W / A / S / D | 移動 |
| マウス | カメラ操作・照準 |
| 左クリック | インク発射 |
| E | インクボム |
| 1 / 2 / 3 | メイン武器切替（連射 / 拡散 / 精密） |
| Space | ジャンプ |
| W + Space（自分色の壁パネル際で） | ウォールクライム（塗った壁パネルをよじ登る） |
| Shift | 自分色のインクに潜伏（高速移動・隠密・急速回復） |
| Q | スペシャルゲージ満タン時にインクバースト |
| Esc | マウスロック解除 |
| R | ゲーム終了後のリスタート |
| `` ` `` (Backquote) | デバッグオーバーレイ表示切替 |
| V | （デバッグ）敵の外見タイプを切替（Speed → Street → Heavy → Technical） |

## ディレクトリ構成

```
index.html              エントリーポイント（importmap で three を解決）
src/
  main.js               起動処理
  config.js             全ての数値パラメータ（バランス調整はここだけ見ればよい）
  style.css             UI スタイル（オリジナルデザイン）
  core/
    Game.js             状態遷移・メインループ・各システムの配線
    InputManager.js      キーボード / マウス / ポインターロック
    CameraController.js  一人称カメラ（マウス/タッチのyaw・pitch制御）
  entities/
    Character.js         Player / EnemyAI 共通の HP・インク・衝突・復活ロジック
    Player.js             プレイヤー入力処理
    EnemyAI.js            CPU の状態機械・操舵・射撃（＋外見タイプの適用・登場演出）
    EnemyAppearance.js    敵の外見バリエーション（4タイプ）の見た目のみを構築
  systems/
    Arena.js              ステージ形状・衝突コリジョン
    PaintSystem.js        塗装グリッド + CanvasTexture 描画
    WallPanel.js           壁パネル1枚分の小さな独立塗装グリッド（ウォールクライム判定用）
    Weapon.js             発射制御（クールダウン・インク消費）
    ProjectileManager.js  インク弾のオブジェクトプール・衝突判定
    ParticleManager.js    エフェクトのオブジェクトプール
  ui/
    UIManager.js          HUD・タイトル・結果画面等の DOM 操作
  audio/
    AudioManager.js       Web Audio API による効果音合成
  vendor/three/           ベンダリングした Three.js（npm registry から取得、CDN 不使用）
```

## ステージの仕組み（塗って移動するマップ）

- 床に加えて、中央高台の上面とランプの斜面も同じ塗装グリッド／テクスチャを共有しており、通常の床と全く同じように撃って塗れる。プレイヤーの移動トレイル・インクサーフ・撃破時の環境ダメージもこれらの面で正しく機能する。
- 中央高台はランプのある面以外、直接歩いて登れないよう側面に衝突コリジョンを追加した。その代わり、高台の西面・南面には「塗れるパネル」（`WallPanel.js` が独立した小さな塗装グリッドを持つ）を設置。足元から頂上まで自分色のインクが連続していれば、パネルに向かって **W + Space** でウォールクライムし、頂上まで登り切れる。ランプ・西パネル・南パネルの3ルートで中央を奪い合う構成。
- 自分色の床で **Shift** を押すとインクへ潜伏する。潜伏中は射撃できない代わりに、高速移動、インクとHPの急速回復、低い当たり判定を得る。静止すると外見と敵マーカーがほぼ消え、移動中は飛沫で居場所が分かる。
- 撃破時は倒した側の大きなインク爆発が足元に残るため、対人戦の勝敗がそのまま陣取りにも影響する。
- 新しく自分の色へ塗り替えたセル数に応じてスペシャルゲージが溜まる。満タン時に **Q**（タッチ操作は **BURST**）でインクを全回復し、周囲へ広がる大きな塗装波と対人ダメージを発生させる。
- メイン武器は連射型の **STREAM**、近距離で横に広く塗る **SPREAD**、低連射・高威力の **PRECISION** の3種。PCでは **1 / 2 / 3** で切り替えられる。サブ武器のインクボムは **E**（タッチ操作は **BOMB**）で使用する。
- 中央高台は旧バージョンより一回り大きくし（`ARENA.platformSize`）、面積そのものの制圧価値を上げた。周囲には遮蔽物を追加し、撃ち合いだけでなく回り込みを誘発する。
- 各スポーン地点の周囲には試合開始時とリスポーン直後に自陣色で塗られた安全地帯（`ARENA.spawnSafeRadius`）が用意され、復帰直後にいきなり中立／敵地に立たされることはない。
- 中央への4方向の進入路には、ジャンプで越えられる腰高の遮蔽物を配置。射線を切って塗り直すか、飛び越えて短距離を詰めるかを選べる。
- CPUは目的地への直進だけでなく、進行方向を複数サンプリングし、自陣インクを優先・敵インクを回避する。特に撤退とインク補給時は安全な塗装ルートを強く選ぶ。

## 現在の制限事項

- ステージは 1 種類のみ（小型アリーナ）
- 塗装可能なのは床・中央高台の上面・ランプ・指定された壁パネルのみ（障害物や一般の壁は不可）
- ウォールクライムはプレイヤー操作のみ対応（CPU は現状クライムせず、地上ルートで迂回する）
- CPU 難易度は「標準」1 段階のみ（`config.js` の `AI` セクションで調整可能な構造は用意済み）
- セーブ／永続化機能はなし（1 セッション完結）
